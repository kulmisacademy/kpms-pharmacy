# Copy to `.env` in the project root (same folder as pubspec.yaml).
# Windows: save as `.env` (not `.env.txt` — use "All files" in Save As, or rename in CMD).
#
# Optional overrides (merged before `.env` on IO platforms):
#   `.env.development` — debug/profile (see `.env.development.example`)
#   `.env.production` — release (see `.env.production.example`)
#
# Use the public anon key only — never service_role in client apps.
SUPABASE_URL=https://ausjypmjhnlvndjokqlw.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImF1c2p5cG1qaG5sdm5kam9rcWx3Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3Nzg1MjA5NjQsImV4cCI6MjA5NDA5Njk2NH0.Vw0wJ8EvTBZLxYgb_xs15tFrEDxHe149SKvkw9j0XQM
